# Secrets
.env

# Generated large data (recreated by scripts/refresh_data.py)
public/data/pcn_icb_mapping.json

# Raw source data
all_practices.json
icb_boundaries_raw.geojson
sicbl_raw.geojson

# Legacy files
fetch_practices.sh
geocode_practices.py
page1.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Reference docs — ignore ad-hoc workbooks, keep the NHS ICB merger source
# file which is load-bearing for scripts/icb_mapper.py (Frimley resolution).
*.xlsx
!ODS+Change+Summary+ICB+Mergers+Phase+1+Apr+2026 (2).xlsx

# Dependencies
node_modules

# Build output
dist
dist-ssr

# OS / editor
.DS_Store
Thumbs.db
.claude/
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.local
.vercel

# ICB merger: patient list size cache (regenerated on 24h TTL, not worth
# committing). The SICBL lookup cache IS committed — it's a tiny stable
# source-of-truth file needed by tests and the sheet-push workflow.
scripts/.patient_list_sizes_cache.json
.secrets/

# Google service-account JSON keys — never commit
nhsjobscraper-*.json
*-sa.json
*-service-account.json
